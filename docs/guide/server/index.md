# Server

Раздел про локальный API: Express 5 на моках, без базы
(`npm run server`, порт 4000).

Пока заготовка — содержимое наполняется по мере того, как появляются решения,
которые стоит объяснить словами.

## Подразделы

- [Раздел 1](/guide/server/section-1) — TODO: о чём.
- [Раздел 2](/guide/server/section-2) — TODO: о чём.

:::tip Как переименовать
Имя файла задаёт адрес: `guide/server/section-1.md` → `/guide/server/section-1`.
Подпись в сайдбаре — отдельно, в `docs/guide/server/_meta.json`.
Переименовали файл — поправьте `name` там же и ссылки выше, иначе
`npm run docs:build` упадёт на проверке мёртвых ссылок.
:::
