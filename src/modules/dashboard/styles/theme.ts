/**
 * Оформление графика. Значения совпадают с токенами приложения из app.css —
 * график не должен выглядеть вставкой из другого проекта.
 *
 * Тема одна: приложение тёмное (`color-scheme: dark`), светлых токенов нет.
 * Раньше палитра выбиралась по системной теме, и при светлой теме ОС
 * на фиолетовой странице оказывались цвета для белого фона.
 */

/**
 * Пять цветов серий. Цвет закреплён за продуктом и не переназначается
 * при фильтрации легендой.
 *
 * Проверено валидатором против поверхности #1b1755:
 *   светлотная полоса, порог насыщенности, контраст к фону — проходят;
 *   худшая соседняя пара при дальтонизме ΔE 8.4 при пороге 8;
 *   худшая пара для обычного зрения ΔE 19.3 при пороге 15.
 */
export const SERIES_COLORS: readonly string[] = [
  '#3987e5',
  '#d95926',
  '#199e70',
  '#c98500',
  '#d55181',
]

export interface Chrome {
  surface: string
  textPrimary: string
  textSecondary: string
  muted: string
  grid: string
  axis: string
  /** линии перекрестия под курсором — заметнее оси, но не спорят с сериями */
  pointer: string
  border: string
}

/** Оси, сетка, подписи и подложка тултипа. */
export const CHROME: Chrome = {
  surface: '#1b1755',
  textPrimary: '#eceaf8',
  textSecondary: '#c4bfe4',
  muted: '#9a93c7',
  grid: 'rgba(255, 255, 255, 0.07)',
  axis: 'rgba(255, 255, 255, 0.16)',
  pointer: 'rgba(255, 255, 255, 0.34)',
  border: 'rgba(255, 255, 255, 0.12)',
}
